# AGENTS.md

## 개발 원칙

- Swift 코드 변경 시 기존 공개 API와 동작 호환성을 우선한다.
- 설계 변경이 필요할 때는 SOLID 원칙을 기준으로 책임 분리, 의존성 방향, 확장 가능성을 검토한다.
- 단순한 Swift Package 구조를 유지하고, 실제 복잡도를 줄이지 않는 추상화는 추가하지 않는다.
- UI 관련 변경은 SwiftUI의 선언적 데이터 흐름을 따르고, 상태 변경 범위를 작게 유지한다.
- 테스트는 변경된 동작을 직접 검증하도록 작성하며, 실행 환경에 따라 결과가 달라지는 로케일, 시간, 난수 의존성은 고정한다.

## PR 리뷰 대응

- PR 리뷰 봇의 지적은 그대로 수용하지 말고, 코드 맥락과 실제 실행 가능성을 확인해 합리적 지적과 환각을 구분한다.
- 합리적 지적이면 수정한 뒤 PR 댓글 또는 대댓글에 무엇을 어떻게 수정했는지 명시한다.
- 지적이 환각이거나 현재 변경 범위에 맞지 않으면 보류 사유를 PR 댓글 또는 대댓글에 명시한다.
- 보류할 때는 단순히 "미반영"이라고 쓰지 말고, 재현 불가, 기존 동작 의도, 공개 API 호환성, 별도 이슈 분리 등 판단 근거를 함께 남긴다.
- 봇 리뷰 대응 댓글에는 최종 상태를 `수정 완료`, `보류`, `추가 확인 필요` 중 하나로 분명히 표시한다.
